<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <!-- MainActivity - Entry Point (LAUNCHER) -->
        <activity
            android:name="com.app.MainActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Material.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LoginActivity -->
        <activity
            android:name="com.app.features.auth.login.activities.LoginActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Material.Light.NoActionBar" />

        <!-- DashboardActivity - Receive data from LoginActivity -->
        <activity
            android:name="com.app.features.dashboard.activities.DashboardActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Material.Light.NoActionBar" />

        <!-- VerificationActivity - Return result via ActivityResultAPI -->
        <activity
            android:name="com.app.features.dashboard.activities.VerificationActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Material.Light.NoActionBar" />

        <!-- ProfileActivity - User profile management -->
        <activity
            android:name="com.app.features.profile.activities.ProfileActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Material.Light.NoActionBar" />

    </application>

</manifest>